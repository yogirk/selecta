# Copy this file to `.env` and fill in the values required for your environment.
# Example:
#   cp .env.example .env
#   # edit .env and update the placeholders

## ---------------------------------------------------------------------------
## Model backend selection
## ---------------------------------------------------------------------------
# 0 -> Gemini API / ML Dev (default)
# 1 -> Vertex AI
GOOGLE_GENAI_USE_VERTEXAI=0

## Gemini API (when GOOGLE_GENAI_USE_VERTEXAI=0)
GOOGLE_API_KEY=

## Vertex AI (when GOOGLE_GENAI_USE_VERTEXAI=1)
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
# Some Google libraries look for REGION; set both for safety.
GOOGLE_CLOUD_REGION=us-central1
# Example Vertex model: publishers/google/models/gemini-1.5-pro
VERTEX_MODEL=

## ---------------------------------------------------------------------------
## Selecta configuration
## ---------------------------------------------------------------------------
# Override the default model if desired.
SELECTA_MODEL=gemini-2.5-pro-preview-03-25
# Path to the dataset descriptor (relative paths resolved from project root).
SELECTA_DATASET_CONFIG=./selecta/datasets/thelook.yaml
# Location of the SQLite persistence database.
SELECTA_DB_PATH=./selecta.db
# Auto-visualisation heuristics (set to false to disable chart suggestions)
SELECTA_AUTOVISUALIZE=true
# Maximum rows to attempt auto visualisation
SELECTA_VIZ_MAX_ROWS=500
# Maximum distinct categories for bar charts
SELECTA_VIZ_MAX_DISTINCT=20

## Optional: path to service account credentials used by BigQuery clients.
GOOGLE_APPLICATION_CREDENTIALS=

## ---------------------------------------------------------------------------
## Service runtime options
## ---------------------------------------------------------------------------
PORT=8080
HOST=0.0.0.0
